server {
    listen 80;
    server_name postgrest;
    location / {
        proxy_pass http://pgrst:3000/;
    }
}

server {
    listen 8000;
    server_name flask;
    location / {
        include uwsgi_params;
        uwsgi_pass unix:/cbsmr/smrpy/socket/uwsgi.sock;
    }
}
